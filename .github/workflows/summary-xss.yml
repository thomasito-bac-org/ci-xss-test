name: summary-xss-test
on:
  workflow_dispatch:
jobs:
  write-summary:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: cat summary_payload.md >> "$GITHUB_STEP_SUMMARY"
